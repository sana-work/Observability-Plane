Option A core execution is now verified.

Do not modify the core sync orchestration or agent execution behavior unless required by a discovered defect.

Implement the next production-hardening increment across both repositories.

Priority 1 — Helm/environment wiring
- Wire every Option A environment variable already defined in code into the appropriate Helm values and deployment templates.
- Follow existing Helm conventions.
- Do not hard-code environment-specific URLs.
- Add documented defaults.
- SYNC_EXECUTOR_BASE_URL must use the internal Kubernetes/OpenShift service DNS pattern.

Priority 2 — Private Executor endpoint protection
- Inspect the existing Service and Route resources.
- Ensure POST /internal/v1/agent-step cannot be reached from the external Route.
- Propose and implement the smallest platform-compatible isolation mechanism.
- Add NetworkPolicy if supported by the current deployment model so Orchestration can reach Executor but untrusted workloads cannot.
- Do not break existing Executor traffic.

Priority 3 — Graceful drain
- Add a drain state to Orchestration and Executor.
- /ready must return not-ready once drain begins.
- New sync work must be rejected while draining.
- Existing work should be allowed to finish.
- Parameterize terminationGracePeriodSeconds.
- Ensure it is configured greater than:
  max sync deadline + preStop/drain allowance + shutdown allowance.

Priority 4 — Observability
Add metrics for:
- active steps by execution_mode
- step duration by execution_mode
- sync capacity rejection
- sync request duration
- sync timeout
- client disconnect
- Executor HTTP failures
- DB pool usage/wait where available
- Kafka consumer lag where available

Reuse the platform's existing metrics/monitoring library if one exists. Do not introduce a new telemetry stack unnecessarily.

For client disconnect:
- keep the existing behavior for this increment;
- classify already-running Executor work as a documented known limitation;
- do NOT implement distributed cooperative cancellation yet.

After implementation:
1. run both unit suites;
2. update OPTION-A-IMPLEMENTATION-STATUS.md;
3. classify each production-hardening item as VERIFIED / NEEDS FIX / NOT TESTED;
4. list exact files/functions changed;
5. produce a staging deployment checklist;
6. do not claim production readiness until mixed async+sync load testing has been executed.